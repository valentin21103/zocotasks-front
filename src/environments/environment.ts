/**
 * Configuración de desarrollo.
 * `apiUrl` sin barra final: los servicios la concatenan con `/api/...`.
 */
export const environment = {
  produccion: false,
  apiUrl: 'http://localhost:5279'
};
