/**
 * Configuración de producción.
 * El build la reemplaza por la de desarrollo vía `fileReplacements` en angular.json.
 */
export const environment = {
  produccion: true,
  apiUrl: 'https://zocotasks-backend.onrender.com'
};
